<h1>Articles</h1>
<ul>
<% @articles.each do |article| %>
  <li><%= link_to article.title, article_path(article.id) %></li>
  <p><%= article.context %></p>
  <% end %>
</ul>
<%= link_to "add pokemon", new_article_path %>
